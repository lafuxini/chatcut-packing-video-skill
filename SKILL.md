---
name: packing-video-smooth-cut
description: Edit vertical packing, journaling, sticker-placement, and order-packing videos into a complete but brisk action montage with 1.5–1.7× motion, tactile audio, gentle blush color, and local-only export.
---

# 打包视频丝滑精剪

## 何时使用

当用户要剪打包、装盒、贴纸、卡片整理、手账或类似俯拍手作视频时使用。尤其适合以下要求：

- 每一个有效放置动作都要出现，但拿下一件物品之间的等待必须剪掉。
- 画面要流畅、密集、有累积感，不能靠删掉核心动作来缩短时长。
- 用户提供了认可的成片或教程，希望沿用它的节奏和滤镜方向。
- 成片必须在 ChatCut Desktop 或本机导出，不能消耗网页端云导出额度。

## 开始前需要什么

- 全部原始视频，优先使用本地原文件。
- 可选的参考成片、教程链接或用户明确认可的旧版本。
- 目标平台与画幅；没有特别说明时保持原始竖屏画幅，不添加左右填充。
- 用户对字幕、音乐、转场的要求；默认不加字幕、音乐和花哨效果。

## 核心剪辑方法

### 1. 先建立动作清单

完整浏览全部素材，列出每一次独特的放置、撕贴、整理、合盖和装饰动作。相同动作有多次拍摄时只保留构图、手势和落点最好的一次，但不能漏掉独特物品。

### 2. 按“接触—放稳”切微片段

每一个放置动作单独成段：

- 入点放在物品即将进入主要操作区，或开始接触桌面、盒子的位置。
- 出点放在物品完全放稳、手开始离开的位置。
- 删除伸手找东西、拿下一件、犹豫、对齐前的长停顿和放稳后的滞留。
- 动作内部若仍在撕、贴、压、抚平或调整，不得误判为空档。
- 相邻动作之间只保留能让眼睛读懂结果的极短停留，通常不超过约 0.1–0.25 秒。

### 3. 节奏与倍速

- 主体动作默认 1.65×，可在 1.5–1.7×之间按复杂度微调。
- 需要看清细节的撕贴、对齐和装饰使用约 1.5×。
- 简单重复放置可使用约 1.65–1.7×。
- 结尾合盖与成品展示使用约 1.5×，并留下 2–5 秒可读的结果。
- 必须验证实际输出速度，不能只改变标签而仍按原速渲染。

### 4. 结构

- 开头用 1–2 秒快速建立桌面、盒子和主题。
- 前段迅速进入第一个清晰动作。
- 中段连续推进物品累积，每次放置只出现一次。
- 后段完整保留最后一批贴纸、卡片、合盖和装饰，不能为了缩短时长整组跳过。
- 如果用户说旧版“前面很好、后面漏了”，锁定认可的前段，只重建后段，并在自然的素材分组边界衔接。

## 画面与声音偏好

- 使用轻柔粉嫩、低对比、不过曝的滤镜，保持肤色和粉色物品层次。
- 做轻度稳定、裁切和曝光一致性修正，不能牺牲手部或物品完整性。
- 默认保持原生竖屏，不添加模糊、白色、粉色或其他人工侧边。
- 做背景降噪，但保留撕纸、塑料袋、贴纸和物品落下的触感声。
- 提升整体响度并限制峰值，避免爆音和明显抽吸感。
- 以直接动作匹配剪切为主；只在必要边界使用极短音频淡化，不加复杂转场。

## 导出规则

- 默认只使用 ChatCut Desktop 本地视频导出或本机渲染。
- 未经用户当次明确同意，不得提交网页端、Agent Plugin 或其他云端视频导出。
- 导出到用户指定的输出文件夹，并使用能区分版本的文件名。
- 保留前一版成片，除非用户明确要求覆盖。

## 完成前检查

逐项执行 [质量检查清单](references/quality-checklist.md)。只有在动作完整、节奏准确、画面无侧边、声音合格且本地文件可播放后，才能报告完成。
